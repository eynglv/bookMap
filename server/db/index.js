const db = require("./database");
// const Book = "./book";
// const Country = "./countries";

//set associations
// Book.belongsTo(Country);
// Country.hasMany(Book);

module.exports = {
  db,
  // Book,
  // Country,
};
